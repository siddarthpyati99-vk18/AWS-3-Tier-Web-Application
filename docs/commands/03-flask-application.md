# Flask Application

The application source is `app/app.py`.

```bash
cd /home/ssm-user/three-tier-app
source venv/bin/activate
pip install -r requirements.txt
```

Replace `YOUR_RDS_ENDPOINT` locally with the actual RDS endpoint.
