# AWS CLI and SSM

```cmd
aws --version
aws sts get-caller-identity
aws ssm start-session --target YOUR_INSTANCE_ID --region us-east-1
```

Use placeholders in public documentation. Never publish credentials.
